/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['housedesigns.co.ke'],
        unoptimized: true,
    },
    trailingSlash: true,
  }
   
  export default nextConfig